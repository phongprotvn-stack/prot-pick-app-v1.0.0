# Phase 2 — Production Polish (Miễn Phí 100%)

**Mục tiêu:** Hoàn thiện app cho production với Firebase Spark + Vercel Free

**Nguyên tắc:** KHÔNG dùng dịch vụ tính phí — chỉ dùng Firestore (500MB free), Vercel (100GB bandwidth free), localStorage fallback.

---

## Task 1: Deploy Firestore Rules + Seed Data

- Publish firestore.rules lên Firebase project `prot-pick-app`
- Seed dữ liệu mẫu vào Firestore để test
- **Cần:** user login Firebase CLI hoặc paste rules qua Console

## Task 2: Fix Mobile Menu (Hamburger)

- Header.tsx có nút hamburger nhưng panel trống
- Thêm mobile drawer với: role toggle, language, theme, tab nav
- Lấy code từ App.tsx gốc (lines 1058-1180)

## Task 3: Git Init

- Khởi tạo git repo tại thư mục dự án
- .gitignore cơ bản (node_modules, dist, .env, .vercel)

## Task 4: Xoá Seed Data Fallback (khi đã có Firebase)

- Sau khi rules publish + seed, data sẽ đọc từ Firestore
- Giữ `src/data.ts` làm seed-only (ko fallback vào localStorage)
- Tối ưu: bỏ initial data khỏi build (giảm bundle size)

## Task 5: Code Splitting

- React.lazy + Suspense cho các tab component
- Giảm bundle JS từ 1MB → 200KB mỗi chunk
- Tận dụng Vite dynamic import

---

## Chi phí = $0

- Firebase Spark Plan: 50K reads/ngày, 20K writes/ngày, 500MB storage
- Vercel Free: 100GB bandwidth/tháng, 6000 build minutes/tháng
- Google Translate API: free (không cần key)
